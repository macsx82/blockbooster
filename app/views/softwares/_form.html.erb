<%= simple_form_for(@software) do |f| %>
  <% if @software.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@software.errors.count, "error") %> prohibited this software from being saved:</h2>

      <ul>
      <% @software.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.association :manufactorer %>
    <%= f.association :operating_system %>
    <%= f.input :description %>
    <%= f.input :year , :collection => 1982..2011%>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
